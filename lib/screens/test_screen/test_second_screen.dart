import 'package:flutter/material.dart';

class TestSecondScreen extends StatelessWidget {
  const TestSecondScreen({
    Key? key,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Hello Test Second Screen"),
    );
  }
}
